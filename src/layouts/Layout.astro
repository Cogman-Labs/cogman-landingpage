---
interface Props {
	title: string
	description: string
}

const { title, description } = Astro.props
import '@fontsource/space-mono/vietnamese.css'
import '@fontsource/space-mono/latin.css'
import '@/styles/global.css'
import { ClientRouter } from 'astro:transitions'
import { SEO } from 'astro-seo'
import { getLangFromUrl } from '@/i18n/utils'

const lang = getLangFromUrl(Astro.url)
---

<!doctype html>
<html lang={lang}>
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<SEO
			title={title}
			titleTemplate="%s :: Cogman Labs"
			description={description}
			canonical={Astro.url.toString()}
			openGraph={{
				basic: {
					title: 'Cogman Labs',
					type: 'website',
					image: 'https://cogmanlabs.com/og-image.jpg'
				},
				image: {
					alt: 'Cogman Labs - AI Engineering Research'
				},
				optional: {
					description: description
				}
			}}
			twitter={{
				creator: '@cogmanlabs'
			}}
		/>
		<link rel="alternate" href={`${Astro.site}en/`} hreflang="en" />
		<link rel="alternate" href={`${Astro.site}vi/`} hreflang="vi" />
		<link rel="sitemap" href="/sitemap-index.xml" />
		<link rel="icon" href="/favicon.svg" type="image/svg+xml" />
		<ClientRouter />
	</head>
	<body>
		<slot />
	</body>
</html>
